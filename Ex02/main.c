#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) 
{
	int x,y,r;
	scanf("%d", &x);
	scanf("%d", &y);
	r = x+y;
	printf("\n\n\n");
	printf("%d + %d = %d",x,y,r);
	return 0;
}

